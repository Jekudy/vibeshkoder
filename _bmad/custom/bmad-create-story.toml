[workflow]
activation_steps_prepend = [
  "Do not read or write sprint-status.yaml. The story file is supporting input; create or update its GitHub Implementation Work issue at handoff.",
]
on_complete = "Execute the mandatory handoff in file:{project-root}/_bmad/custom/github-issue-handoff.md before exiting."
