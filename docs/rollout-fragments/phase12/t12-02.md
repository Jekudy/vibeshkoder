# T12-02 Rollout Fragment

## Summary

Lands the butler evidence builder + canonical context envelope per
`PHASE12_PLAN.md` §3.6 / §4.2 (`ButlerEvidenceContext` frozen dataclass,
`butler_context_hash` helper, `build_butler_evidence` async function). No
user-facing surface flipped this sprint — module is pure foundation for
T12-04 confirm_action and T12-08 evidence-snapshot replay.

## Risk

**LOW.** Internal services-layer module only. No migration. No env vars
added. No flag flipped. No handler wired. All `memory.butler.*` flags
remain default OFF (T12-04 / T12-05 land them). PR merges as a no-op for
production behavior — the new code is unreachable from any registered
handler until downstream sprints.

## Migrations Applied

NONE. (Schema for `butler_plans` / `butler_actions` landed in T12-01 via
migration `073_add_butler_tables.py`.)

## New Files

| File | Purpose |
|------|---------|
| bot/services/butler_evidence.py | `ButlerEvidenceContext` dataclass + `butler_context_hash` + `build_butler_evidence` |
| tests/test_butler_evidence.py | 43 unit tests (frozenness, hash determinism, vanished-row fail-closed, status set, batched call-count, post_init validation) |
| tests/evals/test_butler_leakage.py | 9 Phase 11 binding tests: L11.a (offrecord) + L11.b pre-filter (6 governance-field sub-cases) + L11.b second-line (text + caption) |

## Modified Files

| File | Change |
|------|--------|
| bot/services/governance.py | Added module-level constant `GOVERNANCE_FILTER_VERSION = "phase12-v1"` |
| scripts/lint_privacy_check.sh | Allowlist entries for `tests/evals/test_butler_leakage.py` + `bot/services/butler_evidence.py` |

## Operator Steps

NONE. No rollout action required this sprint. No env var changes, no
Coolify config touch, no flag flip.

## Rollback

Revert PR. Safe — no migration, no env vars, no runtime side effects
(module not imported by any registered handler or scheduler job until
T12-04/T12-05 land).

## Tests

- 43 unit tests in `tests/test_butler_evidence.py` — all green.
- 9 Phase 11 binding tests in `tests/evals/test_butler_leakage.py` — all green.
- Phase 11 binding cumulative: **77 → 86** (+9 from L11.a + L11.b family).
  Final Phase 12 target: 102 at T12-09.

## Hard Constraints Honored

- NO LLM calls (T12-02 doesn't touch `llm_gateway.py`).
- NO read over `#nomem` / `#offrecord` / forgotten / tombstoned content
  (uses `governance.detect_policy` + folded forget/tombstone predicate
  matching `search.py:133`).
- Tombstone match uses `fe.tombstone_key` 3-key prefix (not `target_id`).
- Citations composed via `EvidenceBundle.evidence_ids` (no subclassing).

## Cross-Stream Contracts

- **T12-04** (`confirm_action`) MUST call `butler_context_hash(bundle,
  visibility_scope, governance_filter_version)` with the exact same triple
  stored at plan time → byte-equal `context_hash` for replay verification.
- **T12-08** (evidence-snapshot revalidation) reads
  `butler_actions.evidence_context_hash` and recomputes via the same
  `butler_context_hash` helper. Stale snapshot → action refused.
- **GOVERNANCE_FILTER_VERSION** = `"phase12-v1"` is the versioning anchor
  for the governance-filter predicate. Bump on any change to the
  forget/tombstone/policy fold.

## Scope NOT in This PR (Deferred to T12-06)

- `bot/services/butler_tools/recall_evidence.py` — the tool wrapper that
  binds `build_butler_evidence` into the `ButlerTool` Protocol.

## Docs Touched

- `docs/rollout-fragments/phase12/t12-02.md` (this file, new)
- `docs/memory-system/IMPLEMENTATION_STATUS.md` (Phase 12 in-progress row)

<!-- updated-by-superflow:2026-05-26 -->
