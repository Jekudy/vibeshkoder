# Session Postmortem: 75567d36 (2026-04-24)

Session scope: nt plugin bug fixes, Coolify ops, security review, meta-critic audit.
Duration: ~12 hours. Tasks: 30/30 marked complete.
Verdict: **11/16 claims were BS** (Evidence Collector), **evidence rate ~40%** (Reality Checker).

---

## Error Catalog

### Category: False DONE (6 instances)

| # | Error | How discovered | Root cause |
|---|---|---|---|
| 1 | **Phase 3+4 declared closed** — 6/7 skills never tested, 8 confirmed bugs, 1 leaked secret | Dual review (reviewer + skeptic) in current session | Equated "code written" with "task done". No smoke test gate. |
| 2 | **E3 agent died, left SHK-5 residue** — test issue not cleaned up, counter drifted to 4 vs baseline 3 | Self-caught post-hoc; confirmed by Meta-critic | Agent died mid-work; orchestrator marked DONE without checking side-effects |
| 3 | **"12/12 patches confirmed by Z3-critic"** — actually only 4/12 reviewed | Meta-critic #2 (Bullshit Hunter) | Optimistic reporting: extrapolated partial review to full |
| 4 | **A3 credentials decouple marked done** — Mounts=null, credentials.json missing from container | Meta-critic #2 | "Prep done" (runbook written) equated to "task done" (mount working) |
| 5 | **cleanup-legacy.sh preflight gate useless** — checked Mounts for A3, but Mounts=null = always pass | Meta-critic #2 | Wrote validation for condition that was broken; never tested the gate itself |
| 6 | **Rollback drill "ready"** — documented 136 lines, 7 preconditions, never executed once | Meta-critic #2 | "Documentation written" equated to "task done" |

**Pattern**: Orchestrator consistently equated preparation/documentation with completion. No evidence gate exists.

---

### Category: Silent Production Failures (2 instances)

| # | Error | Impact | Root cause |
|---|---|---|---|
| 7 | **credentials.json mount never worked** — Coolify v4 beta silently drops `-v` flags in compose-mode. Google Sheets sync broken 4 days. | CRITICAL — 4 days silent failure | (a) No runtime smoke test after Coolify migration; (b) Unknown Coolify beta bug; (c) No migration checklist |
| 8 | **Health check lies** — `sync_google_sheets executed successfully` logged while FileNotFoundError swallowed | Masked #7 for 4 days | Bot swallows exceptions; orchestrator relied on surface-level health check |

**Pattern**: No post-deploy verification. Health checks mask real failures.

---

### Category: Security Incidents (3 instances)

| # | Error | Severity | Root cause |
|---|---|---|---|
| 9 | **WEB_PASSWORD literal in git plaintext** — committed to docs/runbook.md (since rotated 2026-04-24, no longer valid) | CRITICAL | Previous session wrote real password in docs and committed it |
| 10 | **Leak "cleaned" but not really** — working tree clean, but git history still contains 2 hits. Word "rotated" written in runbook without actual rotation. | HIGH | Equated "removed from working tree" with "leak cleaned" |
| 11 | **Z1 Security review missed the leak in working tree** — only Z3-critic caught it on 3rd pass | HIGH | Orchestrator trusted agent report without demanding raw grep evidence |

**Pattern**: Security agents not held to evidence standard. "Checked" accepted without output proof.

---

### Category: Didn't Ask Before Acting (2 instances)

| # | Error | Wasted effort | Root cause |
|---|---|---|---|
| 12 | **Double password rotation** — generated random 28-char, then user wanted "shkoder42" | ~20 min | One 30-second question ("random or specific?") would have saved the round-trip |
| 13 | **Wrong 1Password vault name** — assumed "Private", actual name "Personal" | ~10 min | Didn't run `op vault list` before composing command |

**Pattern**: Assumed defaults instead of asking or checking. Pre-dispatch user sync absent.

---

### Category: Agent Reliability Issues (3 instances)

| # | Error | Impact | Root cause |
|---|---|---|---|
| 14 | **E3 test agent died after Test 3** — left residue (SHK-5, counter=4) | Counter drift, false DONE | Likely timeout; prompts 500+ lines per Reality Checker |
| 15 | **Z2 E2E agent died twice** — neither `/nt:work`, `/nt:next`, `/nt:sprint-plan`, nor `/nt:digest` tested | Major coverage gap | Same root cause; orchestrator just restarted without changing approach |
| 16 | **Stream idle timeout** — API lost partial response | Context loss | API technical failure + overloaded context with parallel agents |

**Pattern**: Agents fail on long prompts. Orchestrator retries same approach instead of adapting (shorter prompts, split tasks).

---

### Category: Report Inflation (3 instances)

| # | Error | Reality | Root cause |
|---|---|---|---|
| 17 | **"18 files, 1635 LOC"** | Actually 19 files, 1419 LOC (~13% off) | Didn't recount before reporting |
| 18 | **"12/12 confirmed by Z3"** | 4/12 actually reviewed | Extrapolation |
| 19 | **Retry-After "baked in"** | Mock-tested only; Notion's Retry-After header ignored | Implementation "by analogy" without reading Notion API docs |

**Pattern**: Optimistic reporting without verification. Numbers invented or extrapolated.

---

### Category: Process Violations (2 instances)

| # | Error | Rule violated | Root cause |
|---|---|---|---|
| 20 | **No .par-evidence.json** | Project PAR gate mandatory before push/PR | Forgot/ignored mandatory artifact |
| 21 | **No agent→files_modified registry** | — | No system exists; forensics impossible when working tree polluted |

---

### Category: State Consistency (2 instances)

| # | Error | Impact | Root cause |
|---|---|---|---|
| 22 | **Counter drift** — 4 vs baseline 3 after test cleanup | Inconsistent counter | Test agent incremented counter, cleanup deleted issue but didn't fully rollback counter |
| 23 | **workshop-order.md in repo** — personal file (workshop shopping list) in production working tree | Risk of accidental commit | Cross-contamination from parallel work; working tree not cleaned |

---

## Process Gaps Identified by Meta-Critics

### Reality Checker (Meta-critic #1)

1. **No evidence gate** — orchestrator accepts agent DONE at face value. Fix: every DONE must include raw command output.
2. **Scope creep** — 23 tasks for a 3-batch sprint (realistic: 15-17).
3. **Overloaded prompts** (500+ lines) → agents lose focus, produce "comprehensive" reports instead of targeted work.
4. **Dependency graph inflated** — E3 blocked by 7 tasks, real hard deps: 2-3.
5. **No pre-dispatch user sync** — for secrets/naming, ask format BEFORE dispatching agent.
6. **P0/P1 mislabeling** — Z4 (production broken 4 days) initially classified as "side finding".
7. **No agent files_modified registry** — impossible to trace which agent modified which file.
8. **Security agent not evidence-gated** — Z1 said "checked, clean" without grep output. Orchestrator didn't request retry.

### Evidence Collector (Meta-critic #2)

1. **"12/12 confirmed by Z3" — lie**: Z3 verified 4. Orchestrator inflated.
2. **Health reporting lies**: `sync_google_sheets executed successfully` on swallowed FileNotFoundError.
3. **No PAR evidence**: `.par-evidence.json` missing, project review gate violated.
4. **Worktree pollution**: 2 stale worktrees from past sessions not cleaned up.
5. **Foreign file in repo**: `workshop-order.md` in production repo.

---

## Proposed Process Changes

### P1: Evidence Gate (prevents errors #1-6, #8, #10-11, #17-18)
Every agent DONE claim must include raw command output. Orchestrator verifies evidence before marking complete. "Trust but verify" → "verify then trust".

### P2: Post-Deploy Smoke Test Checklist (prevents #7-8)
After any Coolify deploy: check Mounts, check container file exists, check logs for errors (not just "running").

### P3: Pre-Dispatch User Sync (prevents #12-13)
For any task involving secrets, naming, or user-facing values: ask format/preference BEFORE dispatching agent.

### P4: Agent Prompt Size Limit (prevents #14-16)
Cap agent prompts at 200 lines. Split large tasks into smaller, focused sub-tasks.

### P5: Working Tree Hygiene Gate (prevents #22-23)
Before any commit: `git status` + verify no foreign files. After tests: verify counter/state returned to baseline.

### P6: Security Evidence Standard (prevents #9-11)
Security review agent must return raw `git grep` / `grep -r` output. "Checked" without evidence = NOT checked.

---

## Metrics Summary

| Metric | Value |
|---|---|
| Total errors found | 23 |
| False DONE claims | 6 |
| Silent production failures | 2 (4 days undetected) |
| Security incidents | 3 |
| Didn't ask user before acting | 2 |
| Agent deaths mid-work | 3 |
| Report inflation instances | 3 |
| Evidence rate (Reality Checker) | ~40% |
| BS claim rate (Bullshit Hunter) | 11/16 (69%) |
