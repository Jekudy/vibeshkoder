# Prod status — 2026-09-24

Read-only срез состояния продакшена shkoderbot (Jekudy/vibeshkoder).
Исполнитель: агент devin/swe-2 (thr_vhj35e673u), заказчик — оркестратор
thr_bwjgkj2m6d. Tracking issue: #527. Все проверки read-only: ни одного
деплоя, рестарта или изменения флагов не выполнялось.

## Сводка

| # | Область | Статус | Коротко |
|---|---------|--------|---------|
| 1 | Prod runtime | 🟡 | Жив и здоров, но runtime сменился с Coolify на compose — вся ops-документация устарела; extraction pipeline мёртв 2 месяца |
| 2 | CI | 🟡 | CI/release/healthcheck зелёные; nightly evals молча не запускаются с 2026-07-17 |
| 3 | Issues P0/P1 | 🟡 | 108 дублей алерт-спама + 4 настоящих P0 с 2026-07-16 без движения |
| 4 | main vs deployed | 🟢 | Задеплоен ровно HEAD main (`sha-32cc482`), 0 коммитов behind |
| 5 | Memory-system флаги | 🟡 | Ключевые флаги ON, но extraction фактически сломан; weekly digests ON, но 3/5 последних запусков упали |

---

## 1. Prod status — 🟡

### 1.1 Runtime: Coolify → Compose (документы врут)

PR #522 (`63c8704`, 2026-09-16) перенёс приложения с Coolify на чистый
docker-compose. Фактическое состояние на VPS (`srv1435593`,
187.77.98.73, проверено по SSH 2026-09-24 ~15:30 UTC):

- Проект `shkoder`, `/srv/shkoder/compose.yaml`; контейнеры:
  `shkoder-bot`, `shkoder-web`, `shkoder-db`, `shkoder-redis`.
- **Coolify отсутствует целиком**: нет контейнеров, API на
  `100.101.196.21:8100` и `localhost:8100` — connection refused.
- Volumes переиспользованы от Coolify (`postgres-data-hdazvm5fz836xj9mdyn8c629`,
  `redis-data-gl28f0g5exzzo4k8w0auzygk`, external). Сеть `coolify`
  external — имя историческое.
- `docs/runbook.md`, `CLAUDE.md`, `AGENTS.md` описывают Coolify как prod
  runtime и legacy `/home/claw/vibe-gatekeeper` как rollback — оба
  утверждения устарели (rollback-путь нужно переписать или признать
  утраченным).
- Деплой теперь: `release.yml` → `ops/compose/deploy.py <sha>` — пин
  образов по полному SHA, rollout только bot/web (БД не трогает),
  readiness через `ops/compose/healthcheck.py`.

### 1.2 Контейнеры и доступность — зелёно

- `docker inspect`: все 4 контейнера `restarts=0`; bot/web подняты
  2026-09-18 16:01 UTC, db/redis — 2026-09-16 13:27 UTC.
- `/healthz` внутри bot (`:3000`): `200 {"status":"ok","db":{"ok":true}}`;
  `/healthz/db`: `200 {"db":"ok"}`.
- Публичный `https://cexv50jspo5gl3kq6ojypw43.187.77.98.73.sslip.io/healthz`
  → `200`, тот же payload (обслуживает `shkoder-web:8080` через Traefik
  `vps-proxy`, LE TLS, rate-limits). `/` → `302 /login`.
- Ingestion жив: `chat_messages` за 24 ч — 27 строк.
- VPS: uptime 21 день, load 0.67, RAM avail 1.6 GiB, диск `/` 76%
  (36/48 ГБ, свободно 12 ГБ) — смотреть, но не горит.
- alembic: prod `version_num=092` = голова репо (`092_intro_refresh_waves`).

### 1.3 Логи bot за 24 ч / 7 д — классы ошибок (без тел сообщений)

| Класс | 24 ч | 7 д | Комментарий |
|-------|-----|-----|-------------|
| `extractor: extraction_scheduler_unresolved_run` | 96 | 247 | См. находку F1 — планировщик заблокирован с 2026-07-24 |
| `scheduler: digest_weekly_job window in error state failed` | 1 | 1 | Digest id=21, `DigestCitationValidationError`, admin DM отправлен |
| `llm_gateway: image_description_provider_failed` + `image_memory: photo_description_failed` | 0 | 3+3 | Эпизодические падения vision-провайдера |
| WARNING: `Failed to delete message … for rejected app`, `Failed to DM user … about rejection` | 0 | 1+1 | Разовые ошибки флоу отклонения заявки |

Web-контейнер за 24 ч: 0 строк уровня error. Полный лог bot за 24 ч — 97
строк, все ERROR: prod-логирование на WARNING+, INFO-активность не видна
(ожидаемо, но снижает наблюдаемость).

### 1.4 Бэкапы — зелёно

- `/etc/cron.d/shkoder-pg-backup`: ежедневный дамп 03:17 UTC →
  `/data/coolify/backups/shkoder-postgres/` (путь исторический, живой).
- `/etc/cron.d/vibe-b2-backup` (#523/#524): 03:50 UTC — GPG-шифрование →
  `b2:jekudy-vibe-backups/shkoder/`, retention 30 дней.
- `/var/log/vibe-b2-backup.log`: сегодняшний прогон `OK`,
  `shkoder-pg-20260924T031701Z.dump.gpg` 50 685 583 байта, sha256
  зафиксирован в логе.

### 1.5 Мониторинг — дыры

- В compose healthcheck есть только у db/redis; **bot/web без
  healthcheck** — `restart: unless-stopped` ловит падение процесса, но не
  зависание.
- Публичный `/healthz` обслуживает web и проверяет только web+DB.
  **Polling-цикл бота ничем не пробивается**: при зависшем polling
  healthcheck останется зелёным.
- Новый `healthcheck.yml` (после #522) — только `curl` публичного URL на
  ubuntu-latest раз в 3 ч. Старый composite (Telegram pending updates,
  restart-delta, state-branch history) больше не исполняется; ветка
  `origin/healing-state` заморожена на 2026-09-16.
- Healing-оркестратор выключен флагом `.healing/disabled`
  («synthetic-payload tests revealed orchestrator opens unrelated backlog
  PRs; disabled pending design review») — дизайн-ревью так и не случилось.

---

## 2. CI — 🟡

| Workflow | Статус | Доказательство |
|----------|--------|----------------|
| CI (main) | 🟢 | Последний прогон на `32cc482` 2026-09-18 — success |
| Release Images | 🟢 | success на `32cc482`, образы `sha-*` + alias `:main` в GHCR |
| Healing Healthcheck | 🟢 | Расписание `0 */3 * * *`, все прогоны success с 2026-09-18 по сегодня |
| Evals | 🔴 | `evals.yml` имеет только `workflow_dispatch` — **schedule отсутствует**. Последний реальный прогон 2026-07-17 (event=schedule). Nightly privacy binding suite не исполнялся 69 дней; CLAUDE.md утверждает обратное («nightly evals.yml runs the privacy binding suite», «Binding ACTIVE») — дрейф документации и реальный регрессионный слепой зон |
| PR Issue Gate / Lint Privacy | 🟢 | Работают на PR (видно по сегодняшним прогонам на PR #526) |

Замечание: прогоны «Healing Healthcheck» до #522 ходили по self-hosted
раннеру и Coolify API — смерть Coolify давала красные проверки
09-15/09-16 (`coolify: red Connection refused` в `healthcheck-log.jsonl`).
Сейчас проверка публичная и зелёная, но узкая (см. 1.5).

---

## 3. Issues P0/P1 — 🟡

### 3.1 Алерт-спам

108 открытых issue «[ALERT] healing escalation — healing disabled flag is
present» (всего 112, 4 закрыты). Механика: старый healthcheck падал на
мертвом Coolify API → диспатчил `healing.yml` → оркестратор видел
`.healing/disabled` → эскалация открывала issue на каждый прогон. Окно
спама 2026-07-20 → 2026-09-16; прекратилось после #522 (диспатча больше
нет). Все 108 — дубли одной причины, кандидаты на bulk-close.

### 3.2 Настоящие открытые P0/P1 (без спама)

| Issue | Дата | Суть |
|-------|------|------|
| #415 | 2026-07-16 | [P0] Убрать сырой RAG-протокол из ответов, voice contract |
| #407 | 2026-07-16 | [P0] Структурированное запоминание feedback/improvement |
| #405 | 2026-07-16 | [P0] Сжатие 698 wiki-страниц в canonical wiki + публикация |
| #404 | 2026-07-16 | [P0] Semantic Q&A: embeddings + hybrid retrieval (в работе: #525 + PR #526 сегодня) |
| #409, #410 | 2026-07-16 | [P1] Memory scorecard; контекстный вопрос по reply |
| #424, #487 | 2026-07 | RFC (forwarded message flow; календарь встреч) — не блокеры |
| #525 | 2026-09-24 | Eval hybrid retrieval — открыт PR #526, в работе |

Плюс хвост устаревших открытых тикетов уже смерженной работы
(#197/#198 Phase 5, #164, #132, #118 и др.) — гигиена трекера.

Блокеры: реальных «заблокированных» P0 нет — P0 от 2026-07-16 просто не
двигались 2 месяца (вся активность июля-сентября ушла в инфраструктуру:
миграция на compose, бэкапы, auto-deploy).

---

## 4. main vs deployed — 🟢

- На VPS: `BOT_IMAGE=ghcr.io/jekudy/vibe-gatekeeper-bot:sha-32cc482ab040bb9bf4e4194a465ad5f96b2275a1`,
  web — тот же SHA.
- `git rev-list --count 32cc482..origin/main` = **0** — задеплоен ровно
  HEAD main. Незадеплоенных коммитов нет.
- Последний деплой: 2026-09-18 16:01 UTC (через `ops/compose/deploy.py`).

---

## 5. Memory-system флаги в прода — 🟡

Источник: таблица `feature_flags` prod-БД (только имена и состояния) +
имена env-переменных контейнера bot.

### ON
`memory.ingestion.raw_updates`, `memory.extraction.scheduler`,
`memory.candidates.auto_promote`, `memory.images.description`,
`memory.qa.enabled`, `memory.qa.llm_synthesis`,
`memory.digests.weekly`, `memory.wiki.compiler`,
`memory.wiki.static_publish`

### OFF
`memory.import.apply`, `memory.qa.semantic` (с 2026-07-17 — ждёт #404),
`memory.digests.daily` (с 2026-07-22), `memory.wiki.enabled`

### Не развёрнуты вовсе
`memory.butler.*` и `memory.graph.*` отсутствуют в `feature_flags`;
`NEO4J_*` env не заданы → Phase 10/12 не задеплоены.

### Эффективное состояние ≠ флагам

- **F1. Extraction сломан при включённом флаге.** Последний run
  `deab253c` 2026-07-24 06:29 UTC — `run_status=failed`,
  `gateway_error=provider_transient:5xx`, окно курсора 9785→9786,
  `attempt_no=1`. Строки в `extraction_run_resolutions` нет →
  `_has_unresolved_cursor_run` (`bot/services/extractor.py:1374`)
  находит failed-run без resolution и блокирует каждый тик планировщика
  (~96 ERROR/сутки). Последний candidate/card — 2026-07-23
  (`extraction_candidates` 760, `knowledge_cards` 760). Pipeline
  knowledge cards фактически мёртв 2 месяца. Класс бага известен:
  carryover #261 чинил running-row leak; здесь — failed-run без
  resolution.
- **F2. Weekly digests ON, но ломаются.** `digests`: id=17 posted
  (08-27), 18 failed `DigestProviderError` (09-03), 19 failed
  `DigestCitationValidationError` (09-10), 20 posted (09-17), 21 failed
  `DigestCitationValidationError` (09-24). 3 из 5 последних — падения;
  админу уходит DM при каждом сбое.
- **F3.** `memory.wiki.enabled=f`, но `wiki.compiler` и
  `wiki.static_publish` = t, `CLOUDFLARE_*` env заданы — публичная
  вёрстка wiki формально выключена, пайплайн компиляции/публикации
  работает (проверить, не публикует ли static_publish в CF Pages при
  выключенном member-wiki — по замыслу флаги независимы, но связку стоит
  зафиксировать в доке rollout).
- **F4.** В env bot только `WEB_PASSWORD` + `WEB_SESSION_SECRET`;
  `WEB_ADMIN_PASSWORD`/`WEB_MEMBER_PASSWORD` не заданы → prod до сих пор
  на legacy-алиасе одного пароля (сплит ролей T9-03 в прода не включён).
- Прочее: в env-файле болтаются устаревшие `COOLIFY_*` переменные —
  безвредны, но путают.

---

## Чтобы довести до прода — по порядку

| # | Шаг | Риск |
|---|-----|------|
| 1 | Разблокировать extraction: для run `deab253c` добавить строку в `extraction_run_resolutions` (или официальный retry-путь, если он предполагает resolution-first), затем понаблюдать 1–2 тика. Завести issue на баг-класс «failed run без resolution навсегда блокирует курсор» — retry-логика должна сама закрывать или эскалировать, а не молча стоять 2 месяца | Низкий–средний: одна audit-строка, но сначала прочитать семантику `ExtractionRunResolution` — неверная resolution может сдвинуть курсор мимо данных |
| 2 | Разобрать `DigestCitationValidationError` (2 падения за 3 недели) — вероятен дрейф citation-контракта на живых данных; `DigestProviderError` — квота/таймаут провайдера. Сначала read-only диагноз по `error_text`/`digest_runs` | Низкий (диагноз), средний при правках renderer-пути |
| 3 | Вернуть nightly в `evals.yml` (добавить `schedule:` обратно) или осознанно зафиксировать manual-only и поправить CLAUDE.md | Низкий |
| 4 | Bulk-close 108 issue-спама одним комментарием-томбстоуном + закрыть причину (`.healing/disabled` — либо чинить оркестратор, либо удалять подсистему целиком) | Нулевой (гигиена) |
| 5 | Добавить compose healthcheck для bot/web + пробинг polling-цикла (например, bot-side heartbeat в `/healthz`, который устаревает без апдейтов) — сейчас зависший polling неотличим от живого | Низкий–средний: healthcheck должен быть нехрупким, чтобы не перезапускать зря |
| 6 | Переписать `docs/runbook.md`/`CLAUDE.md` под compose-реальность; решить судьбу rollback-пути (legacy `/home/claw` удалён/неактуален) | Нулевой для доков; отсутствие rollback-плана — принимаемый риск, который надо озвучить |
| 7 | Перевести prod на `WEB_ADMIN_PASSWORD` + `WEB_MEMBER_PASSWORD` если member-wiki будет включаться | Низкий: рестарт web + коммуникация членам |
| 8 | Диск 76%: убедиться, что `docker-image-prune`/`docker-builder-prune` cron реально работают; алерт на >85% | Низкий |
| 9 | `image_description_provider_failed` ×3/нед — проверить квоту/модель vision-провайдера, если image-memory важна | Низкий |
| 10 | Чистка env: убрать `COOLIFY_*` из `bot.env`, решить про `COOLIFY_API_TOKEN` в `~/.env.tokens` (сервиса больше нет) | Нулевой |

## Что НЕ проверено (честно)

- Содержимое последнего posted-дайджеста (20) и качество текста — только статус.
- Фактическая доступность CF Pages wiki (проверил только наличие env-имён).
- Telegram UX-чек `/start` — не делал (read-only, но чувствительно: отправка сообщения боту).
- Состояние self-hosted раннера `shkoder-vps` (больше не нужен новым healthcheck, но может быть жив для ручного `healing.yml`).
- Полный объём `docker logs` ограничен json-file ротацией 10MB×3 (~7 дней истории).
